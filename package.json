{
  "name": "cqut-auth",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @cqut/oidc-op dev",
    "dev:oidc": "pnpm --filter @cqut/oidc-op dev",
    "dev:demo": "pnpm --filter @cqut/demo-site dev",
    "generate:env": "node scripts/generate-deploy-env.mjs",
    "generate:env:stdout": "node scripts/generate-deploy-env.mjs --stdout",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier --write ."
  },
  "pnpm": {
    "overrides": {
      "lodash": "^4.18.0",
      "path-to-regexp": "^8.4.0"
    }
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^6.0.2"
  }
}
